var express = require('express');
var router = express.Router();

/* GET API's. */
//TODO: disabled till have auth functionality available here
//router.get('/', function(req, res) {
//  res.redirect('/login');
//});

module.exports = router;
